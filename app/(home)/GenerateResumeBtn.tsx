"use client";
import React from "react";
import Button from "../components/Button";

type Props = {};

const GenerateResumeBtn = (props: Props) => {
  return <Button title="Generate Resume" onClick={() => {}} />;
};

export default GenerateResumeBtn;
